const container = document.querySelector(".container");
const closeBtn = document.querySelectorAll('.close-btn'); 

// This script is no longer needed for toggling as navigation is handled by server-side rendering and standard <a> links.
// You can add other client-side logic here if needed in the future.